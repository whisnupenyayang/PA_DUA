import 'package:flutter/material.dart';

class TokoKopiPage extends StatelessWidget {
  const TokoKopiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lokasi Toko Kopi'),
      ),
      body: Center(
        child: Text('Lokasi '),
      ),
    );
  }
}
